<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <PropertyGroup>
    <!-- Underscore here ensures the stub loads first -->
    <AssemblyName>0PipLibStub</AssemblyName>
    <ProjectGuid>{4697851B-C8A3-47DC-9665-B61EF7DB1530}</ProjectGuid>
  </PropertyGroup>
  <Import Project="..\PipLib.Macros.props" />

  <PropertyGroup>
    <DocumentationFile>$(OutputPath)\$(AssemblyName).xml</DocumentationFile>
    <RootNamespace>PipLib.Stub</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="$(SolutionDir)\PipLibStub.Inject\PipLibStub.Inject.csproj" />
  </ItemGroup>

  <Target Name="InjectModuleInitializer" AfterTargets="AfterBuild">
    <Exec Command="$(SolutionDir)\PipLibStub.Inject\bin\$(Configuration)\PipLibStub.Inject.exe $(TargetPath)"></Exec>
  </Target>

</Project>
